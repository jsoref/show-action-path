name: Test
on:
  push:

jobs:
  action-path:
    runs-on: ubuntu-latest
    steps:
      - uses: jsoref/show-action-path@main
  action-subpath:
    runs-on: ubuntu-latest
    steps:
      - uses: jsoref/show-action-path/test@main
  local-path:
    runs-on: ubuntu-latest
    steps:
      - uses: ./
  local-subpath:
    runs-on: ubuntu-latest
    steps:
      - uses: ./test
